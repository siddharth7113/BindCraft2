# --- Python build / bytecode ---
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/

# --- Environment built by install.sh (uv-managed, ~4 GB) ---
.venv/
.bindcraft-tools/

# --- Local caches kept in-repo (see env.sh): AF2 params 5.3 GB + XLA graphs ---
cache/

# --- Scratch (TMPDIR) and campaign outputs ---
tmp/
results/
bindcraft_*.out

# --- Target structures: downloaded from RCSB and prepared by script (see LOGBOOK.md) ---
targets/

# --- Protenix prediction outputs: 80 MB of .cif, regenerated by run_protenix*.sh in ~18 min ---
analysis/protenix/out/
